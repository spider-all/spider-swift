# spider-swift

Crawl GitHub APIs and store the discovered orgs, repos, commits...

### Swift

``` bash
swift package init --type executable
```

### Run in terminal

``` bash
swift run
```

### Run in docker

``` bash

```
